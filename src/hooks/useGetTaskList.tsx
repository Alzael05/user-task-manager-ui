import { useEffect } from "react";

const useGetTaskList = () => {
  useEffect(() => {
    // Fetch task list from API or perform any necessary logic here
  }, []);
};

export default useGetTaskList;
